To use MySQL:
In application.properties;
Comment the h2 datasource section if uncommented.
Uncomment the mysql datasource section if commented.
Create schema/database, username, password according to the properties.

To use H2:
Comment the mysql datasource section if uncommented.
Uncomment the h2 datasource section if commented.
